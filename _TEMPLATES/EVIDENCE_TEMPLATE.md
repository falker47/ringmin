# Evidence

## Environment

```text
repository_head=
platform=
python=
dependency_source=requirements.txt|editable|other
task_mode=STANDARD|STRICT
```

## Claim ledger

| Claim | Classification | Evidence | Independent? | Limitation |
|---|---|---|---|---|
| | | | | |

## Commands and checks

| Command/check | Exit/result | What it checks | What it does not check |
|---|---|---|---|
| | | | |

Do not paste a command from README or CI as though it was executed. Record exact output or a concise reproducible excerpt.

## Artifact and provenance checks

- artifact path:
- generating source/command:
- input/version:
- generation commit:
- schema/hash:
- independent verifier:
- reproducibility limitation:

Use `not applicable` rather than omitting this section for tasks that do not touch artifacts.

## Failed checks and negative evidence

Record failures, counterexamples, abandoned lemmas, and why they matter.

## Final diff inspection

- `git status --short`:
- complete `git diff` inspected:
- untracked additions inspected directly or with a no-index diff:
- direct whitespace check for untracked additions:
- `git diff --check`:
- protected paths unexpectedly changed:
- generated files unexpectedly changed:

## Residual uncertainty

State exactly what remains unproved, uncertified, untested, environment-dependent, or hosted-CI-dependent.
