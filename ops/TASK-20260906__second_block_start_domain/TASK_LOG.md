# Task Log

Append entries; retain failed attempts and negative evidence.

## 2026-09-06 12:32 +02:00 — Startup and exact discriminator

- Base HEAD: d169dd86b8aaadd19a822528e53c29f88bb9bd6f; branch main;
  existing origin https://github.com/falker47/ringmin.git.
- The only untracked file is the user-supplied request image under
  .codex-remote-attachments/. It is task input, not unrelated work;
  no tracked changes or staged changes preceded this task.
- Read the root contract, canonical index and current status, relevant
  fixed-order ledger/roadmap entries, the complete start proof, pertinent
  width and second-block formulas, exact baseline definitions/brackets,
  preceding start dossier/checker and all three dossier templates.
- Some combined read outputs were truncated; focused reads recovered
  the pertinent definitions, formulas and task records. Plain Git reads
  encountered the ownership guard. Repository-scoped safe.directory
  reads succeeded; no persistent Git configuration was changed. Git's
  personal-ignore-file warning did not prevent the successful reads.
- Mode STRICT; expected delta is the eight paths in TASK_STATUS.md.
- Analytic discriminator before computation: v is strictly increasing
  in the block coordinate, and v(epsilon/2)<1. Thus at least the first
  half is chord. Its translated derivative minus the chord-diagonal
  derivative is positive except at the midpoint. The mixed-chain
  contribution is also strictly positive.
- Plan a proof valid for every A>0, 0<u<u+epsilon<A/3, then use only
  the imported rational admissibility margins to specialize the baseline.
  No new interval root enclosure or width-minimum checker is needed.
- Diagnostic budget fixed before execution: mpmath at 70 digits, three
  positive A values and three normalized starts, at most 180 bisections
  per implicit root, all-chord/mixed/entry-tie samples, and fixed finite
  difference steps. Scans are falsification diagnostics only. No agents,
  optimization, finite recovery or geometric computation are involved.

## 2026-09-06 12:46 +02:00 — Proof, checks and source handoff

- Proved the sign for arbitrary A>0 and 0<u<u+epsilon<A/3 using
  only exact algebra and the chord first half. Derived the explicit
  epsilon^3/[48*(A+u+epsilon)^2] derivative lower bound.
- Classified all-chord, mixed and upper-endpoint entry ties; proved
  C^1 matching using dominated difference quotients. Retained both
  physical endpoints and the switch cost term before cancellation.
- Defined the two distinct touching reflections at u=lambda and proved
  weak/cost continuity on the closed triangle. Integrated the positive
  derivative and supplied the interior sequence proving the reverse
  infimum inequality. No boundary-width optimization was performed.
- New Fraction checker exited 0 with three positive admissibility
  margins. No radical or switch-root gates remain. The previous local
  checker also exited 0 as a supplementary regression, not a dependency
  of this proof.
- The independent 70-digit diagnostic exited 0: 54 identities at 27
  pairs, 36 smooth and 18 entry-tie central differences, 27 derivative
  lower bounds, 27 strict fixed-width boundary comparisons, 18 switch
  slope limits and 27 boundary continuity probes. It imports only
  mpmath and uses rational scale/start probes, never proxy definitions
  of the exact baseline. All outputs remain numerical observations.
- Source review clarified the positive-width qualifier for z>epsilon/2
  and the branchwise use of clipped-switch velocities.
- A Git prefix using PowerShell PWD failed the ownership check; the
  combined shell ended at a successful date command, so its overall
  exit 0 did not certify the failed Git subcommands. Repeated the actual
  diff/status commands with the explicit repository-scoped option and
  per-command exit checks; all exited 0. No math checker failed.
- Full proof/checker read and complete tracked diff inspected. The
  independent stdlib source audit exited 0: eight allowed paths,
  tracked/untracked whitespace, AST and Fraction-only import, five
  proof links, one owning ledger and sixteen protected texts equal HEAD.
- Recorded literal diagnostic/audit programs, outputs and source hashes.
  The completed dossier receives a final full read and the same audit
  before staging. Source state is READY_FOR_REVIEW; commit and normal
  push follow under the existing authorization.
- Exactly one next atomic task: independent review of the domain sign,
  switch regularity and boundary-infimum theorem, recording acceptance
  or corrections without boundary-width optimization or finite transfer.

## 2026-09-06 — Staged inspection before integration

- Read the completed dossier/current status in full and reran the source
  audit: exit 0, identical source hashes and all protection checks pass.
- The sandbox granted the index write. `git add --` on exactly the eight
  inspected paths exited 0. The entire cached diff was then read in two
  explicit path groups, covering all eight files without truncation.
- `diff --cached --check` and `diff --exit-code` exited 0 with no diff
  output. Status showed only eight staged task paths plus the unchanged,
  excluded request image. This append and the matching evidence paragraph
  are the final inspected documentation additions before restaging.
- State remains READY_FOR_REVIEW. Authorized commit and normal push to
  origin/main are integration, not independent mathematical acceptance.
