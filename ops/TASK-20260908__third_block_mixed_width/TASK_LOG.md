# Task Log

Append-only chronology.

## 2026-09-08 — Startup and expected delta

- Requested and observed HEAD b21c2dff20ca7419db56545c67386b369b8d24ac;
  clean tree, main, existing origin=falker47/ringmin.
- Read AGENTS.md, PROJECT_KNOWLEDGE.md, CURRENT_STATUS.md, relevant
  fixed-order ledger and roadmap entries, third-width proof/checker/dossier,
  exact boundary-minimum proof/checker, boundary measure definitions,
  task templates and historical publication metadata/claim excerpts.
- STRICT; eight-path expected delta stated before editing. Previous proofs,
  finite/global results and arXiv-v1 assets are protected.
- Ordinary Git reads initially failed dubious ownership, exit 1. A
  command-local safe.directory restores reads without changing configuration;
  personal-ignore permission warnings remain nonfatal.

## 2026-09-08 — Analytic resolution and two location gates

- The boundary-width argument depends only on A/4<v<A/3, so its mixed
  derivative/curvature proof applies with the third block's fixed start v.
  Re-derived the switch term and both width derivatives. The mixed
  curvature exceeds 131/120 while the entry derivative is negative and
  the upper-endpoint derivative is positive: monotonicity fails once.
- The already accepted epsilon_b distance bound implies epsilon_b<7/160;
  this is a rational weakening of that theorem, not a recomputed input.
- A fixed two-corner Fraction calculation at 29/5000 and 67/10000 gives
  respectively negative and positive endpoint signed squares. The analytic
  distance bound then gives Delta_*<27/4000<h. Both gates passed first try;
  there was no width scan, root solution, integral enclosure or input fit.
- Rationalized chord bounds and strict decrease give a quantitative saving
  over 1/250 and a rational enclosure of the continuous cost difference.

## 2026-09-08 — Proof, bounded checks and durable memory

- Wrote the complete mixed derivative, the nonzero curvature switch term,
  entry/endpoint signs and unique-minimum proof. Both exact location gates
  and the cost bounds are rational; no approximation defines a root.
- New check_mixed_width.py, prior check_width.py and check_boundary.py each
  exit 0. Exact commands/output are in EVIDENCE.md. Full pytest, finite
  certificate verification and paper build are inapplicable to this delta.
- Updated the single owning fixed-order ledger, roadmap and current status.
  One patch was rejected before application for duplicate operations on
  CURRENT_STATUS.md; retried as single-file edits, with no mathematical loss.
- Next within the same task: complete source/protection/whitespace audit,
  record the final evidence, then stage, inspect, commit and push normally.

## 2026-09-08 — Source audit and integration handoff

- Read the complete new proof/checker, dossier and three-file tracked diff.
  Corrected status wording to say the derivative strictly increases;
  the proof and checker did not change after their verification.
- Literal source audit in EVIDENCE.md exits 0: eight allowed paths, four
  proof links, sole owner, Fraction-only AST with exactly two gates and no
  loops/floats, explicit untracked whitespace and 17 protected texts equal
  the accepted baseline. Recorded proof/checker SHA256 hashes.
- State READY_FOR_REVIEW; repeat audit on final records, stage only the
  inspected paths, inspect the complete staged diff and whitespace, then
  commit and push to existing origin/main. External acceptance is separate.
- Exactly one next atomic task: independent review of this mixed-width
  theorem and bounded checker, stopping before finite/geometric transfer.

## 2026-09-08 — Staged verification

- Repeated source audit exits 0 with unchanged proof/checker hashes.
- Explicit eight-path staging exits 0 with tool-granted escalation.
- Complete staged diff inspected in separate parts; reread EVIDENCE.md
  alone to cover truncation of one aggregate tool output.
- Cached whitespace and absence of unstaged differences each exit 0.
  Final record-only edits are inspected, restaged and checked before
  normal commit/push. No mathematical source changed after verification.
