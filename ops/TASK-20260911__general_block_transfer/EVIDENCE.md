# Evidence

## Environment

    repository_head=372f96c0a9f14d968e7ecb3b1a039e7867340005
    platform=Windows; PowerShell
    python=3.14.3
    task_mode=STRICT

## Claim and check scope

Fourth-block analytic proof is internally validated; its old checker is bounded
exact-arithmetic support, not a finite recovery or geometric certificate.
The general recovery proof and its all-pairs root transfer have now passed
separate internal mathematical review. They yield U_4; the owning global
entry records the precise saving and scope.

## Commands and results

- `python --version`: exit 0, Python 3.14.3.
- Per-invocation safe.directory Git status: clean; branch main; HEAD above.
- Read-only Drive search of Review State Registry: accepted baseline c45162f,
  updated 2026-09-11T14:04:06Z. No external state mutated.
- Reviewer subagent runs and detailed mathematical scope are recorded in
  [INTERNAL_REVIEW.md](INTERNAL_REVIEW.md). The new normal/-O checker covers
  30034 small orders, 18 rational-surrogate large orders, 1660560 total cells,
  342 directed full-max probes, 438 panel probes and four corruption controls;
  both runs exit 0 with identical output. Parent inspected the complete
  checker and independently rederived its reversal-list vs cell-formula
  separation, branch-free cost bounds and negative-control semantics.
  All computation is local; hosted CI not inspected.

## Artifacts and provenance

No certificate or publication artifact regenerated. Existing progress-log
portability debt is recorded by the implementation ledger and remains for the
goal's later reproducibility checkpoint.

## Failed checks and negative evidence

Initial ordinary Git calls failed ownership checks under the sandbox identity.
Explicit per-invocation safe.directory enables reads without global config edits.

## Final diff inspection

Completed before integration: all 23 task files inspected in full; staged blobs
match inspected working content; tracked/staged whitespace checks exit 0.
Explicit UTF-8/final-newline/whitespace checks cover every addition; 37 local
Markdown link targets exist. Initial naive link scan matched fenced code and
was corrected to exclude code spans. Protected paths are absent from the diff.

## Residual uncertainty

No new finite/global optimality claim. Global asymptotic limit and coefficient
characterization remain candidates until proved and adversarially checked.
