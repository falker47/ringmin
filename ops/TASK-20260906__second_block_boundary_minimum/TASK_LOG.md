# Task Log

Append entries; do not erase failed attempts or contradictory evidence.

## 2026-09-06 — Startup and read-only analysis

- Base HEAD: 648a8eae98c987dbcc11aa28fdcf55cb946d0ef8, existing main/origin.
- Mode: STRICT; exact continuous mathematics and reproducible proof checks.
- Read AGENTS.md, PROJECT_KNOWLEDGE.md, CURRENT_STATUS.md; the relevant
  fixed-order ledger and roadmap entries; the prefix lambda/alpha/joint
  definitions, second-block variation, width and start-domain notes;
  the pertinent predecessor dossier/checkers and all three templates.
- Plain Git reads hit the ownership guard. Command-local safe.directory
  resolved it; no persistent Git configuration was changed. Some combined
  source reads exceeded the display limit and were followed by focused reads.
- Tracked tree was clean. The only untracked path was the predecessor
  request image under .codex-remote-attachments/. An early clarification
  was requested while all work remained read-only. Inspection of the
  image established that it is precisely the related start-domain and
  boundary-reduction request, already excluded by the predecessor dossier.
  That classification resolved the ambiguity; the user was informed that
  a further decision was unnecessary. The image remains untouched.
- Expected delta: nine task paths; all pre-existing proofs, old dossiers,
  publication/certification/production assets and other ledgers protected.
- Mathematical discriminator: prove a negative all-chord derivative,
  strictly increasing mixed derivative and positive upper endpoint slope.
  Locate entry with rational signs and bound the distance to stationarity
  analytically. The supplied diagnostic bracket is not an assumption.

## 2026-09-06 — Derivation and exploratory checks

- Derived closed cost/derivative primitives, exact entry cancellation,
  clipped-switch one-sided derivatives and full branch partition.
- Proved mixed D_b''>1 analytically, Psi_b(h)>11*h/280 and
  D_b(h)>11*h^2/1440. These give uniqueness, attainment and a single
  positive-width cost zero without numerical value or slope enclosures.
- Derived 0<epsilon_b-tau_b<tau_b^2/(8*B). Two directed rational entry
  comparisons then suffice for 43/1000<epsilon_b<11/250.
- A preliminary 40-digit approximate-input secant solve failed its
  convergence tolerance (exit 1); no result was accepted from it.
  A bounded-bisection replacement exited 0 and was explicitly diagnostic.
  Its x=0.28765 was an exploratory input, never an exact definition.
- A standalone Fraction-only checker executed from stdin exited 0.
  The separate 70-digit program recomputed both baseline parameters from
  their defining equations, then passed all bounded raw-max, derivative,
  curvature, endpoint and entry checks (exit 0). Decimal results remain
  numerical observations; no computation constructs a finite permutation.

## 2026-09-06 12:01 UTC — Authoring

- Added the authoritative boundary proof, minimal rational checker and
  independent diagnostic source. Added this dossier using the templates.
- The proof imports only the existing baseline definitions/brackets and
  the boundary measure/start theorem. The old u=1/3 width minimum and
  its refined alpha gates are not dependencies.
- No previous proof, publication, certificate, generated result or
  production path was edited. Verification and durable-memory updates
  remain within this same atomic task.

## 2026-09-06 — Verification and handoff preparation

- The authored exact checker and independent 70-digit diagnostic both
  exited 0. Complete outputs, budgets and limitations are in EVIDENCE.md.
- A notation collision between the endpoint function and a primitive
  parameter was removed. The positive switch-denominator argument was
  made explicit. A checker docstring now says mixed curvature instead of
  the potentially ambiguous convexity. No executed calculation changed.
- The completed proof, both scripts and dossier were read directly;
  the complete three-file tracked diff was inspected. The source audit
  was rerun after the completed evidence: exit 0, identical hashes,
  nine allowed paths, seven proof links, one owner and seventeen
  protected texts equal HEAD. Tracked and untracked whitespace pass.
- Two authoring patch attempts were rejected without modifying their
  targets (duplicate path operation and out-of-order hunks). Single
  update patches resolved them; details are retained in EVIDENCE.md.
- Updated only the owning ledger, current status and roadmap. The next
  atomic task is independent review of the boundary theorem and checker.
- State set to READY_FOR_REVIEW for the authorized stage/commit/push
  sequence. External acceptance and hosted CI remain separate.

## 2026-09-06 — Staged review and terminal integration

- The sandbox denied the first index write (exit 1, index.lock permission).
  The same nine-path git add was granted escalation and exited 0.
- git diff --cached --check and git diff --exit-code exited 0 with no
  diff output. The complete cached diff was inspected in explicit path
  groups, with a focused dossier read after display truncation.
- Status showed exactly nine staged task files and the excluded related
  request image. This final record is inspected and restaged before the
  containing commit. The final handoff records commit/push verification.
- Final state: READY_FOR_REVIEW. Exactly one next atomic task remains
  independent review of this boundary theorem and its minimal checker,
  without finite recovery or geometric transfer.
